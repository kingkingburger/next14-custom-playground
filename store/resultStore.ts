export const useCheckResult = (store: any) => {
  const showMessage = async (
    type: "insert" | "update" | "remove",
    isPushInfo: boolean = true,
    isShow: boolean = true,
  ) => {
    let result = false;
  };
  return { showMessage };
};
